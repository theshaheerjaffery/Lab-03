#include <iostream>
using namespace std;
int main () {

    bool isHot=true;
    bool isSunny=false;
    cout<<"isHot AND isSunny is: "<<(isHot && isSunny)<<endl;
cout<<"isHot OR isSunny is: "<<(isHot || isSunny)<<endl;
cout<<"NOT isHot is: "<<(!isHot);

    return 0;
}